export type ListItem = {
    id: number,
    mark: string,
    descr: string
}

export type Platforms = {
    id: number,
    img: string,
    alt: string
}

export type FormData = {
    name: string,
    email: string,
    file: File,
    checkbox: boolean
}